# JWT 签名密钥（生产环境务必修改为强随机字符串）
JWT_KEY=SuperSecretKeyForJwtAuth2024!

# MinIO 对象存储
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Redis（默认无密码，docker-compose 内部通信无需配置）
# REDIS_PASSWORD=

# 部署端口
BACKEND_PORT=5000
FRONTEND_PORT=8080
